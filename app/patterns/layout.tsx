import { patternsMetadata } from './metadata'

export const metadata = patternsMetadata

export default function PatternsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
